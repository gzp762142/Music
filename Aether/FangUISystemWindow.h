#import <UIKit/UIKit.h>

/// System-level window (TrollEngine SHMainWnd / FloatingBallUIWindow style).
/// Requires Music entitlements: accessibility-window-hosting / platform / no-sandbox.
@interface FangUISystemWindow : UIWindow
@end
